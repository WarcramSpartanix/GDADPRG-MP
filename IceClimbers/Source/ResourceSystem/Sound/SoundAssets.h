#pragma once
#include "Core/TypeAlias.h"

namespace SoundNames
{
	const String ACCEPT = STRINGIFY(ACCEPT);
	const String SELECT = STRINGIFY(SELECT);
}

namespace SoundFilepaths
{
	const String ACCEPT = "Assets/SFX/acceptSFX.wav";
	const String SELECT = "Assets/SFX/selectSFX.wav";
}
