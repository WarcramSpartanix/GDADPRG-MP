#pragma once
#include "Core/TypeAlias.h"

namespace SceneNames
{
	const String TEST = "TEST_SCENE";
	const String TITLE = STRINGIFY(TITLE);

	// TODO: Would our level be generated from a file or create them through code which requires different scenes?
	const String LEVEL = STRINGIFY(LEVEL);

	const String RESULT = STRINGIFY(ResultScene);

};